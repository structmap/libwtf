[project]
name = "libwtf-browser-chrome-interop"
version = "0.1.0"
description = "Chrome WebTransport interop test harness for libwtf"
requires-python = ">=3.11"
dependencies = [
    "selenium>=4.20",
]

[tool.uv]
package = false
